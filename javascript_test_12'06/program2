/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var searchRange = function(nums, target) {
  var count=0;
  var a=[];
  for(var i=0;i<nums.length;i++)
  {
      if(nums[i]==target)
      {
         a.push(i);
         count++;
      }

  }  
  if(count>0)
  {
      return a;
  }
  else
  {
      return[-1,-1];
  }
};
